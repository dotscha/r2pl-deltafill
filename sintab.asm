	lda #lo(sin_offset+39)
	sta sintab,y
	iny
	lda #lo(sin_offset+40)
	sta sintab,y
	iny
	lda #lo(sin_offset+41)
	sta sintab,y
	iny
	lda #lo(sin_offset+42)
	sta sintab,y
	iny
	lda #lo(sin_offset+43)
	sta sintab,y
	iny
	lda #lo(sin_offset+44)
	sta sintab,y
	iny
	lda #lo(sin_offset+45)
	sta sintab,y
	iny
	lda #lo(sin_offset+46)
	sta sintab,y
	iny
	lda #lo(sin_offset+47)
	sta sintab,y
	iny
	lda #lo(sin_offset+48)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+49)
	sta sintab,y
	iny
	lda #lo(sin_offset+50)
	sta sintab,y
	iny
	lda #lo(sin_offset+51)
	sta sintab,y
	iny
	lda #lo(sin_offset+52)
	sta sintab,y
	iny
	lda #lo(sin_offset+53)
	sta sintab,y
	iny
	lda #lo(sin_offset+54)
	sta sintab,y
	iny
	lda #lo(sin_offset+55)
	sta sintab,y
	iny
	lda #lo(sin_offset+56)
	sta sintab,y
	iny
	lda #lo(sin_offset+57)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+58)
	sta sintab,y
	iny
	lda #lo(sin_offset+59)
	sta sintab,y
	iny
	lda #lo(sin_offset+60)
	sta sintab,y
	iny
	lda #lo(sin_offset+61)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+62)
	sta sintab,y
	iny
	lda #lo(sin_offset+63)
	sta sintab,y
	iny
	lda #lo(sin_offset+64)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+65)
	sta sintab,y
	iny
	lda #lo(sin_offset+66)
	sta sintab,y
	iny
	lda #lo(sin_offset+67)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+68)
	sta sintab,y
	iny
	lda #lo(sin_offset+69)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+70)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+71)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+72)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+73)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+74)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+75)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+76)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+77)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+78)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+77)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+76)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+75)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+74)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+73)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+72)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+71)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+70)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+69)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+68)
	sta sintab,y
	iny
	lda #lo(sin_offset+67)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+66)
	sta sintab,y
	iny
	lda #lo(sin_offset+65)
	sta sintab,y
	iny
	lda #lo(sin_offset+64)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+63)
	sta sintab,y
	iny
	lda #lo(sin_offset+62)
	sta sintab,y
	iny
	lda #lo(sin_offset+61)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+60)
	sta sintab,y
	iny
	lda #lo(sin_offset+59)
	sta sintab,y
	iny
	lda #lo(sin_offset+58)
	sta sintab,y
	iny
	lda #lo(sin_offset+57)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+56)
	sta sintab,y
	iny
	lda #lo(sin_offset+55)
	sta sintab,y
	iny
	lda #lo(sin_offset+54)
	sta sintab,y
	iny
	lda #lo(sin_offset+53)
	sta sintab,y
	iny
	lda #lo(sin_offset+52)
	sta sintab,y
	iny
	lda #lo(sin_offset+51)
	sta sintab,y
	iny
	lda #lo(sin_offset+50)
	sta sintab,y
	iny
	lda #lo(sin_offset+49)
	sta sintab,y
	iny
	lda #lo(sin_offset+48)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+47)
	sta sintab,y
	iny
	lda #lo(sin_offset+46)
	sta sintab,y
	iny
	lda #lo(sin_offset+45)
	sta sintab,y
	iny
	lda #lo(sin_offset+44)
	sta sintab,y
	iny
	lda #lo(sin_offset+43)
	sta sintab,y
	iny
	lda #lo(sin_offset+42)
	sta sintab,y
	iny
	lda #lo(sin_offset+41)
	sta sintab,y
	iny
	lda #lo(sin_offset+40)
	sta sintab,y
	iny
	lda #lo(sin_offset+39)
	sta sintab,y
	iny
	lda #lo(sin_offset+38)
	sta sintab,y
	iny
	lda #lo(sin_offset+37)
	sta sintab,y
	iny
	lda #lo(sin_offset+36)
	sta sintab,y
	iny
	lda #lo(sin_offset+35)
	sta sintab,y
	iny
	lda #lo(sin_offset+34)
	sta sintab,y
	iny
	lda #lo(sin_offset+33)
	sta sintab,y
	iny
	lda #lo(sin_offset+32)
	sta sintab,y
	iny
	lda #lo(sin_offset+31)
	sta sintab,y
	iny
	lda #lo(sin_offset+30)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+29)
	sta sintab,y
	iny
	lda #lo(sin_offset+28)
	sta sintab,y
	iny
	lda #lo(sin_offset+27)
	sta sintab,y
	iny
	lda #lo(sin_offset+26)
	sta sintab,y
	iny
	lda #lo(sin_offset+25)
	sta sintab,y
	iny
	lda #lo(sin_offset+24)
	sta sintab,y
	iny
	lda #lo(sin_offset+23)
	sta sintab,y
	iny
	lda #lo(sin_offset+22)
	sta sintab,y
	iny
	lda #lo(sin_offset+21)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+20)
	sta sintab,y
	iny
	lda #lo(sin_offset+19)
	sta sintab,y
	iny
	lda #lo(sin_offset+18)
	sta sintab,y
	iny
	lda #lo(sin_offset+17)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+16)
	sta sintab,y
	iny
	lda #lo(sin_offset+15)
	sta sintab,y
	iny
	lda #lo(sin_offset+14)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+13)
	sta sintab,y
	iny
	lda #lo(sin_offset+12)
	sta sintab,y
	iny
	lda #lo(sin_offset+11)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+10)
	sta sintab,y
	iny
	lda #lo(sin_offset+9)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+8)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+7)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+6)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+5)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+4)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+3)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+2)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+1)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+0)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+1)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+2)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+3)
	sta sintab,y
	iny
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+4)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+5)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+6)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+7)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+8)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+9)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+10)
	sta sintab,y
	iny
	lda #lo(sin_offset+11)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+12)
	sta sintab,y
	iny
	lda #lo(sin_offset+13)
	sta sintab,y
	iny
	lda #lo(sin_offset+14)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+15)
	sta sintab,y
	iny
	lda #lo(sin_offset+16)
	sta sintab,y
	iny
	lda #lo(sin_offset+17)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+18)
	sta sintab,y
	iny
	lda #lo(sin_offset+19)
	sta sintab,y
	iny
	lda #lo(sin_offset+20)
	sta sintab,y
	iny
	lda #lo(sin_offset+21)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+22)
	sta sintab,y
	iny
	lda #lo(sin_offset+23)
	sta sintab,y
	iny
	lda #lo(sin_offset+24)
	sta sintab,y
	iny
	lda #lo(sin_offset+25)
	sta sintab,y
	iny
	lda #lo(sin_offset+26)
	sta sintab,y
	iny
	lda #lo(sin_offset+27)
	sta sintab,y
	iny
	lda #lo(sin_offset+28)
	sta sintab,y
	iny
	lda #lo(sin_offset+29)
	sta sintab,y
	iny
	lda #lo(sin_offset+30)
	sta sintab,y
	iny
	sta sintab,y
	iny
	lda #lo(sin_offset+31)
	sta sintab,y
	iny
	lda #lo(sin_offset+32)
	sta sintab,y
	iny
	lda #lo(sin_offset+33)
	sta sintab,y
	iny
	lda #lo(sin_offset+34)
	sta sintab,y
	iny
	lda #lo(sin_offset+35)
	sta sintab,y
	iny
	lda #lo(sin_offset+36)
	sta sintab,y
	iny
	lda #lo(sin_offset+37)
	sta sintab,y
	iny
	lda #lo(sin_offset+38)
	sta sintab,y
	iny
	rts
